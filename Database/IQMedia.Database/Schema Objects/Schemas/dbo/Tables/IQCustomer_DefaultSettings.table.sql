CREATE TABLE [dbo].[IQCustomer_DefaultSettings] (
    [_CustomerGuid] UNIQUEIDENTIFIER NOT NULL,
    [Field]         VARCHAR (150)    NOT NULL,
    [Value]         VARCHAR (MAX)    NOT NULL
);

